import photosLogo from '../../assets/photos-logo.png';
import photosIcon from '../../assets/photos-icon.png';
import chatLogo from '../../assets/chat-logo.png';
import chatIcon from '../../assets/chat-icon.png';
import aichatLogo from '../../assets/aichat-logo.png';
import aichatIcon from '../../assets/aichat-icon.png';

const assetMap = {
  photos: { logo: photosLogo, icon: photosIcon },
  chat: { logo: chatLogo, icon: chatIcon },
  aichat: { logo: aichatLogo, icon: aichatIcon },
};

export const ecosystemApps = [
  {
    key: 'aichat',
    label: 'AI Chat',
    url: 'https://aichat.vegvisr.org'
  },
  {
    key: 'chat',
    label: 'Chat',
    url: 'https://chat.vegvisr.org'
  },
  {
    key: 'photos',
    label: 'Photos',
    url: 'https://photos.vegvisr.org'
  },
  {
    key: 'contacts',
    label: 'Contacts',
    url: 'https://contacts.vegvisr.org',
    icon: 'https://favicons.vegvisr.org/favicons/1773834325586-1-1773834331342-32x32.png'
  },
  {
    key: 'connect',
    label: 'Connect',
    url: 'https://connect.vegvisr.org'
  },
  {
    key: 'vemail',
    label: 'Email',
    url: 'https://vemail.vegvisr.org'
  },
  {
    key: 'legacy',
    label: 'Legacy',
    url: 'https://www.vegvisr.org/user'
  }
];

export const getAppKeyFromHost = (host = '') => {
  if (!host) return null;
  if (!host.endsWith('vegvisr.org')) return null;
  const [subdomain] = host.split('.');
  return subdomain || null;
};

export const getAssetForApp = (appKey, type = 'logo') => {
  if (!appKey) return null;
  const entry = assetMap[appKey];
  if (!entry) return null;
  return entry[type] || null;
};
